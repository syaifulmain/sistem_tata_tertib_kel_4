document.addEventListener('DOMContentLoaded', () => {
    // Tombol Tambah Laporan
    document.getElementById('tambah-laporan').addEventListener('click', () => {
        window.location.href = 'tambah_laporan.php';
    });

    // Event listeners atau interaksi JavaScript tambahan dapat ditambahkan di sini
});
